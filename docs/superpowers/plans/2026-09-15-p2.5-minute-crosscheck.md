# P2.5 遗留包（受保护分钟线 + 跨源校验 + 护栏拆分）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. **注意：本会话 dispatch 通道已实锤死亡（P2 先例），由 controller 亲自按 SDD 逐任务实现+自审执行。**

**Goal:** 落地 P2.5 spec r3 三块：G3 universe 护栏拆分+L9 5xx 零重试（先行）、G1 受保护按需分钟线、G2 跨源交叉校验（Tushare 主/东财辅，默认关）。

**Architecture:** minute_path 新模块聚合分钟交互面（令牌桶/熔断/ETL 互斥/双缓存）组合 data_source 原语与 CacheFacade；cross_check 新模块 provider 协议 + 日抽样对账，独立锁与调度；bars_etl 护栏从"整轮冻结"拆为"仅新码深回补冻结"。

**Tech Stack:** Python/FastAPI + SQLAlchemy + APScheduler + Redis(redis-pydantic 无关裸 client) + pytest/ruff/mypy；前端 Vue3 + vitest。

## Global Constraints

- 接口冻结按 spec §10 I1–I9；常量按 I7 逐字：`MINUTE_L1_TTL=15, MINUTE_L2_FRESH=120, MINUTE_BREAKER=900, MINUTE_RPS=1.0, MINUTE_BURST=3, MINUTE_MAX_COUNT=320, CROSS_CHECK_SAMPLE=30`，另 `MINUTE_L1_MAX_KEYS=512, MINUTE_L1_SWEEP_EVERY=50, CROSS_CHECK_CRON="mon-fri 15:35", CROSS_CHECK_DEFER_TRIES=3, CROSS_CHECK_DEFER_SLEEP=300`。
- 既有 563 项后端测试 + 211 项前端测试基线零修改全绿（facade 策略表触碰既有断言时，修测试须 commit message 单列理由）。
- 不造数/不自动改数/降级不静默；501 与 5xx：ETL 与分钟面全部零重试立即失败。
- market_bars 零迁移、分钟线零落库、cross_check 结果零落表。
- UI 中文；保留既有 API 字段名（全部新增键）。
- 门禁命令（每任务末尾）：`python -m pytest tests/ -q --no-cov -o addopts=""`、`python -m ruff check --fix backend tests`、`python -m ruff format backend tests`、`python -m mypy backend`。

---

### Task 1: G3 护栏拆分 + L9 5xx 零重试 + abortReason

**Files:**
- Modify: `backend/bars_etl.py`（resolve_universe/_do_run/_default_fetch/bars_health）
- Test: `tests/test_bars_etl_run.py`（追加）、`tests/test_bars_etl.py`（追加）

**Interfaces:**
- Produces: `resolve_universe() -> tuple[list[str], list[str], list[str]]`（market,user,scan 三面，**签名变更**——唯一调用方 `_do_run` 与本模块测试）；`bars.abortReason: str|None` 进 `bars_health()`；`_default_fetch` HTTPError 零重试。
- Consumes: 无新前置。

- [ ] **Step 1: 红测试——护栏拆分三面语义**（追加到 `tests/test_bars_etl_run.py`；沿用该文件既有 fixture 风格：`_FakeFetch`/`monkeypatch.setattr(bars_etl, "resolve_universe", ...)` 等，先读文件头部对齐 helper 名）

```python
def test_guard_splits_new_vs_existing(monkeypatch):
    """market 面小→只冻结新码深回补；存量码深补+轻队照常。"""
    # industry 表空（market=[]），但库内已有 cov-a/cov-b（stale_deep），用户在 watchlist
    # 断言：backfill 队列仅含存量码；新码 missing 全进 deferred；abortReason 记 market_universe_small
    ...

def test_guard_small_market_keeps_daily_and_abort_reason(monkeypatch):
    """market=0 user=2 存量=0：本轮仅日补用户码，backfill deferred 如实，不炸不冻结。"""
    ...
```

（实现时按文件内既有 run_full 测试的构造方式补全断言体——复用 `_rows/_bar/fake fetch` helpers。）

- [ ] **Step 2: 跑红** `python -m pytest tests/test_bars_etl_run.py -q --no-cov -o addopts=""` → FAIL（TypeError: resolve_universe 不返回三元组）

- [ ] **Step 3: 实现**
  - `resolve_universe()`：分别查 industry_map 表→`market`；watchlist∪trade_plans→`user`；`SELECT DISTINCT code FROM market_bars WHERE adjustment=''`→`existing`；返回 `(market, user, sorted(set(market)|set(user)|set(existing)))`。docstring 记 scan 含存量码理由（护栏触发时库内资产不腐烂）。
  - `_do_run`：`stats.universe = len(scan)`；新全局 `_market_size`；`UNIVERSE_MIN` 判断改 `if len(market) < UNIVERSE_MIN: stats.abort_reason = "market_universe_small"`（不 return）；backfill 队列拆两档：`existing_bf = [c for c in missing|deep if c in set(existing)]`（不受限、预算优先）+ `new_bf = [...不在 existing]`（护栏开→全 deferred）；**预算 BACKFILL_CODES_PER_RUN 对两档统一适用，存量档占先**；daily/light 无条件；小分母 abort 老路径（scan 全空）保留。
  - `EtlStats` 增 `abort_reason: str = ""`；`run_full` finally 里 `_abort_at["reason"] = stats.abort_reason or None`（新 dict 全局，60s health 缓存复用现 `_h_value` 通道：直接把 reason 塞进 bars_health 值）。
  - `bars_health()` 返回 dict 增 `"abortReason": stats级最近一次（含 None→键存在为 null）`。
  - L9：`_default_fetch` 改直调带零重试语义——捕获 `requests.HTTPError` → 立即 re-raise（不 sleep 不重试）；实现面：在 `_default_fetch` 包一层，或在 data_source 增 `_http_get(..., retry_http_error=False)` 参数（默认 True，仅 bars_etl 传 False）——**选后者**，分钟线 T3 复用同参数。测试：

```python
def test_default_fetch_no_retry_on_501(monkeypatch):
    import requests
    calls = []
    def boom(*a, **k):
        calls.append(1)
        raise requests.HTTPError("501 Server Error")
    monkeypatch.setattr(bars_etl.data_source, "fetch_json", boom)
    # 经 _default_fetch：HTTPError 立即抛且 fetch 面只被调 1 次（retry 循环未启用）
```

- [ ] **Step 4: 绿** + 全基线 `python -m pytest tests/ -q --no-cov -o addopts=""`（resolve_universe 既有测试改三元组解包=**语义变更修测试**，commit message 理由：spec §3 D4 显式改签名）
- [ ] **Step 5: 门禁 + 提交** `feat: G3 护栏三面拆分 + L9 5xx零重试 + abortReason`

---

### Task 2: CacheFacade 前缀策略表（minute: 白名单）

**Files:**
- Modify: `backend/redis_cache.py`
- Test: `tests/test_redis_cache.py`（追加）

**Interfaces:**
- Produces: `WHITELIST_PREFIXES = ("quotes:", "history:", "minute:")`；`PREFIX_POLICY: dict[str, PrefixPolicy]`（fresh_window getter / px_floor / stale_ok）；`get/stale_read/take_stale/set` 公开签名零变化（I6）。
- Consumes: 无。

- [ ] **Step 1: 红测试**（`tests/test_redis_cache.py` 追加；复用该文件 FakeClock+fakeredis 风格）

```python
def test_minute_prefix_fresh_window_is_120s_not_global_ttl(fake_clock): ...
    # 全局 ttl_getter=8 时，minute: 键 100s 龄仍新鲜；quotes: 键 100s 不新鲜（行为不变）
def test_minute_stale_read_always_none(...):  # take_stale/stale_read 对 minute: 恒 None（R6 语义）
def test_minute_px_is_ttl_plus_60_no_1860_floor(...):  # set minute:→ px≈(120+60)*1000
def test_quotes_history_behavior_byte_identical(...):  # 参数化回归钉：两前缀现行为不变
```

- [ ] **Step 2: 跑红**（minute: 键当前被 `_eligible` 拒）
- [ ] **Step 3: 实现**：`PrefixPolicy = namedtuple("PrefixPolicy", "fresh px_floor stale_ok")`；`_policy(key)` 按前缀查表：quotes/history→None 哨兵走旧全局路径（**不改一行现逻辑分支**），minute→(120, 180_000, False)。`_read_envelope`/`get`/`set` 各 3 行分派；`stale_read` 入口 `if not policy.stale_ok: return None`。
- [ ] **Step 4: 绿**（既有 redis_cache 22 项零修改）
- [ ] **Step 5: 门禁 + 提交** `feat: CacheFacade 前缀策略表——minute: 入白名单、120s 窗、无降级读`

---

### Task 3: minute_path 模块（护栏核心）

**Files:**
- Create: `backend/minute_path.py`
- Test: `tests/test_minute_path.py`（新建，全离线 fake）

**Interfaces:**
- Produces:
  - `fetch_minute(code: str, period: str, count: int, index: bool = False, *, now: Callable[[], float] = time.time) -> MinuteFetch`
  - `@dataclass MinuteFetch: bars: list[dict]; source: str | None; state: str; degraded: bool`（state ∈ ok|etl_busy|circuit_open|unavailable|rate_limited；source ∈ upstream|cache_l1|cache_l2|None）
  - `breaker_state(now=None) -> str`（"closed"|"open"）；`reset_for_test()`
  - `load_minute_kline(code, period, count, index=False) -> list[dict]`（纯上游+解析，注入面，I1）
- Consumes: `data_source.fetch_json(url, params, retry_http_error=False)`（T1 参数）；`redis_cache.CacheFacade`（T2 策略表）；`bars_etl._RUN_LOCK`。

- [ ] **Step 1: 红测试骨架**（fake 注入全离线；`now` 显式传入驱动桶/半开/窗）

```python
def make(fake_fetch, facade=None, lock=None) -> ...  # 组装被测（monkeypatch 模块级 _fetch_raw/_facade/etl_locked）
def test_period_whitelist_and_count_clamp(): ...
def test_bucket_burst3_then_429_state_rate_limited(): ...
def test_etl_lock_held_zero_upstream_state_etl_busy(): ...
def test_three_fails_open_900s_single_probe_halfopen(): ...
    # 开路→窗内仅 1 个探测请求（第 2 个并发请求不吃上游）；探测失败→重开 900s
def test_501_single_fail_instant_open(): ...
def test_l1_hit_back_and_15s_expiry(): ...
def test_l1_sweep_and_512_cap(): ...
def test_l2_minute_no_stale_read(): ...
def test_parse_mkline_rows_shape(): ...  # '202609150935' → '2026-09-15 09:35' + OHLC float
```

- [ ] **Step 2: 跑红**（ImportError）
- [ ] **Step 3: 实现**（顺序即纪律，spec §4）：

```python
def fetch_minute(code, period, count, index=False, *, now=time.time) -> MinuteFetch:
    period = _norm_period(period)          # 白名单，越界 ValueError→端点层转 422
    count = max(10, min(int(count), MINUTE_MAX_COUNT))
    key = f"minute:{('idx:' if index else '')}{symbol}:{period}:{count}"
    if bars_etl_run_lock_locked():
        return _from_cache_only(key, "etl_busy") or MinuteFetch([], None, "etl_busy", True)
    if not _bucket.take(now):
        return MinuteFetch([], None, "rate_limited", False)
    if _breaker.opened(now):
        probe = _breaker.acquire_probe(now)   # single-flight 半开探测
        if not probe:
            return _from_cache_only(...) or MinuteFetch([], None, "circuit_open", False)
    cached 两级读（L1 15s / facade.get）→ 命中即回
    上游 _fetch_raw（失败：5xx/501 立即计败，网络类零重试——retry_http_error=False 已在 T1）
    → breaker 记账（成功且探测中→闭路；失败→计败/501 立即开）→ 回退缓存 → MinuteFetch
```

  breaker 内部：`_fails:int; _open_until:float; _probe_inflight:bool`。L1：`_l1: dict[str, tuple[float, list]]`、写计数 %50 清扫 + `len>512` 逐出最旧。日志名 `atlas.minute_path`，事件 `minute_ok(debug)/minute_fail(warning)/minute_circuit_open|close/minute_etl_defer/minute_rate_limited`。
- [ ] **Step 4: 绿**
- [ ] **Step 5: 门禁 + 提交** `feat: minute_path 受保护分钟线核心——桶/熔断半开单探/ETL互斥/双缓存/L1逐出`

---

### Task 4: /api/minute 端点 + MinuteOut + health 两键

**Files:**
- Modify: `backend/schemas.py`（MinuteBar/MinuteOut + HealthOut `minuteCache/minuteCircuit`）、`backend/app.py`（端点 + health 接线）
- Test: `tests/test_backend_api.py`（追加，不动既有区域）

**Interfaces:**
- Consumes: T3 `fetch_minute`（period 归一抛 ValueError）；T2 facade `state()`。
- Produces: `GET /api/minute?code&period&count&index` → MinuteOut（HTTP 恒 200，state 承载降级；VALIDATION_ERROR 422 白名单越界/code 非法；RATE_LIMITED 429 桶空）。`/api/health` 增 `minuteCache`（有 facade 时 `connected|bypassed|down`，无=down）、`minuteCircuit: closed|open`。

- [ ] **Step 1: 红测试**：422（period=7）、state 透传（monkeypatch minute_path.fetch_minute 返回固定 MinuteFetch →断言 body 逐字段）、429→RATE_LIMITED 契约、health 两键在既有键零触碰（键集合等式断言）。
- [ ] **Step 2: 红** → **Step 3: 实现**（端点 ~25 行；错误契约复用 `ERR_*` 常量与 detail 形状）→ **Step 4: 绿** → **Step 5: 门禁 + 提交** `feat: /api/minute 端点与 health 分钟线双键`

---

### Task 5: cross_check 模块（provider 协议 + 对账）

**Files:**
- Create: `backend/cross_check.py`
- Test: `tests/test_cross_check.py`（新建，fake provider）

**Interfaces:**
- Produces:
  - `run(provider: str | None = None, *, date: str | None = None, sleep: Callable[[float], None] = time.sleep) -> CrossStats`
  - `@dataclass CrossStats: provider:str|None; sampled:int; mismatched:list[str]; missing:list[str]; skipped_suspended:int; status:str`（ok|degraded|disabled|deferred_etl_running|no_samples）；`last_result() -> dict|None`（health 用）；`EastmoneyProvider/FakeTushareProvider` 实现 `fetch_close_map(date) -> dict[str, tuple[float|None, float|None]]`。
- Consumes: T1 三面 universe；`market_bars` bfq 读；`settings.tushare_token`；`data_source` 网络原语（EM 面 `fetch_json` retry_http_error=False）。

- [ ] **Step 1: 红测试**

```python
def test_tushare_units_vol_hands_and_amount_thousand(): ...   # 换算断言（×100/×1000）
def test_close_threshold_rel_and_abs_both_must_break(): ...   # 0.09% 大价差 vs 0.15% 分厘差 两个边界
def test_zero_vol_days_not_compared(): ...
def test_provider_row_missing_counts_skipped_suspended_not_mismatch(): ...
def test_db_row_missing_counted_missing(): ...
def test_sampling_anchors_plus_date_salt_reproducible(): ...  # 同 date 两次抽样相等；锚定码必在
def test_token_empty_routes_eastmoney_token_set_routes_tushare(): ...
def test_etl_lock_held_returns_deferred_and_keeps_last_result(): ...  # 顺延 3 次逻辑经 fake sleep
```

- [ ] **Step 2: 红** → **Step 3: 实现**：EM provider=`push2 ulist.np` secids 批（≤100/请求，fltt=2 直取浮点；f2='-'/None→skipped 语义）；Tushare provider=`ts.pro_bar`/`pro_api(token).daily(trade_date=YYYYMMDD)` 单次全市场 + `600000.SH`→`600000`；换算注释块写 T0 实测记录；`last_result` 全局 60s 缓存对象 + `cross_check_logger`；**无自动改数路径存在即违规**。→ **Step 4: 绿** → **Step 5: 门禁 + 提交** `feat: cross_check Tushare主/东财辅对账核心——单位换算与分账判据`

---

### Task 6: 调度第四 id + health.bars.crossCheck + CROSS_CHECK_ENABLED

**Files:**
- Modify: `backend/settings.py`（`cross_check_enabled: bool = Field(default=False, alias="CROSS_CHECK_ENABLED")`）、`backend/bars_etl.py`（register_jobs 第 4 spec 调 cross_check.run；未启用不注册——import 面 lazy 防环）、`backend/app.py`（health.bars dict 里挂 crossCheck=last_result()|null）
- Test: `tests/test_bars_etl.py`（FakeScheduler 断言第 4 job kwargs；开关关→3 job）、`tests/test_backend_api.py`（health bars.crossCheck 键形状）

- [ ] Step 1 红 → Step 2 实现 → Step 3 绿（563 基线回归） → Step 4 门禁 + 提交 `feat: 跨源校验 15:35 调度与 health 接线（CROSS_CHECK_ENABLED 默认关）`

---

### Task 7: 前端 ViewStockDetail 分钟周期（受保护交互）

**Files:**
- Modify: `frontend/src/api/client.ts`（fetchMinute）、`frontend/src/views/ViewStockDetail.vue`（周期按钮组 1/5/15/30/60 + 状态条）、必要时 `frontend/src/stores/useQuotesStore.ts` 或组件本地态（**选组件本地态**，不进 store——无轮询无共享）
- Test: `frontend/src/views/__tests__/`（或既有就近）vitest：按钮渲染、state→黄标/灰条/toast 映射、重试按钮单次调用、escapeHtml 不破

- [ ] Step 1 vitest 红 → Step 2 实现（中文文案按 spec §6 逐字）→ Step 3 `npx vitest run` 211+新增全绿 + `npx vue-tsc --noEmit` → Step 4 `npx eslint frontend/src --max-warnings 0` + prettier → Step 5 提交 `feat: 详情页分钟线周期切换与降级态呈现（无轮询手动触发）`

---

### Task 8: Live 冒烟 + 文档收口 + 全支门禁

- [ ] 冒烟（`.superpowers/smoke_p25.py`）：①EM cross_check 真实一次（token 空分支，`CROSS_CHECK_ENABLED=1` 注入跑 `cross_check.run()`，六字段落 json；mismatched 逐码 log）；②minute live：mkline DNS 实测（web3 不可解→circuit/unavailable 如实链路 live 走通即验收 R1；若通→画线数据人工核 1 码）；③护栏拆分 live：模拟 industry=0（monkeypatch 表查询）跑 1 轮 drain 证存量/用户码不被冻结（**只读 DB 场景用事务回滚或独立假表注入，禁污染生产库**）；④回补进度快照（codes/rows/deferred）入 json。
- [ ] Tushare 2b 独立验收项：**记 BLOCKED-token**（无 token 不强验；spec 允许 disabled 合入），报告如实。
- [ ] AGENTS.md：布局加 `minute_path.py`/`cross_check.py`、测试文件 2 新行、存储说明补 minute: 白名单与 crossCheck 位、计数 563→终数。ROADMAP：两待办打勾（含 live 证据行）。
- [ ] 台账 + `python .superpowers/sdd.py package` 终评 → 全门禁（含前端）。
- [ ] 提交 `docs: P2.5 冒烟证据与文档收口`。

---

## Self-Review 记录（plan 对照 spec）

- G1→T2/T3/T4/T7 ✓；G2→T5/T6/2b=T8 ✓；G3+L9→T1 ✓；I1–I9 全覆盖 ✓；R7 逐出→T3 ✓；半开单探→T3 ✓；deferred 区分→T5/T6 ✓；前端时距/重试→T7 ✓；分钟线不落库/零迁移→全局约束 ✓。
- 类型一致性：MinuteFetch.state 枚举与 T4 端点/ T7 前端映射同源（spec §4 逐字）；CrossStats.status 含 no_samples（T5 实现细节，health status 枚举同步）。
- 风险：T1 改 resolve_universe 签名 → 既有测试改动列入"语义变更修测试"白名单（spec D4 依据）。
