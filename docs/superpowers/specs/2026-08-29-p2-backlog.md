# P2/P3 Backlog（2026-08-29 头脑风暴产出 + 追加决策）

P1（统一交易台 UX 落地）完成后按此推进。每项启动前仍需走 spec → plan → execute 循环。

## P2 量化能力与通知

1. **通知中心（方案 A，2026-08-29 用户已选定）**
   - 系统事件以 `kind=system` 写入现有提醒中心（服务器持久化），底部导航"执行"角标统一计数；toast 保留为即时反馈。
   - 需要后端新增系统提醒写入支持；提醒中心列表加类型筛选。
   - 触发场景：工作区冲突自愈（409 自动采用/覆盖）、保存失败、行情降级（stale/error）、数据源故障切换。
   - 拒绝的替代方案：前端会话级通知中心（刷新丢历史、与提醒中心重叠）。
2. 行情失败时缓存降级兜底（stale-on-error cache fallback）。
3. MarketBar 本地日线历史的读取路径（回测/走势离线可用）。
4. 追加风险指标（回撤持续期、胜率、单格收益分布等）。
5. 全市场选股器探索（当前 curated ~50 股票池，需评估数据源与配额）。
6. 新策略类型（设计文档既有 backlog，不动现有网格）。

## P3 架构质量

1. 拆分 `index.html` / `app.js` / `styles.css`（视图组件化、样式分文件；顺带清理 P1 遗留的死样式类）。
2. `node:test` 为纯函数模块补测试（现无 JS 测试运行器）。
3. 统一 API 错误契约（`detail.error` 之外的结构化错误码）。
4. 行情缓存 TTL 下限与外部接口限频。
